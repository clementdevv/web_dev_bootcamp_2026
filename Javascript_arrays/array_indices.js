// Elements in an array can be accessed using their indices (values used to identify their positions)

let days = ['Monday', 'Saturday', 'Wednesday'] 

console.log(days[2])

days[1] = "Sunday"
console.log(days[1]) // becomes Sunday instead of Saturday since we have changed the element at that position

days[6] = 77
console.log(days)